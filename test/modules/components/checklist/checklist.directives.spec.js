'use strict';
/*
describe("Checklist Suite", function() {

    beforeEach(module('checklist'));
    //beforeEach(module('app/modules/components/checklist/checklist.html'));

    describe("Directives Tests", function() {

        var _scope;
        var _element;
        var _sut;

        beforeEach(inject(function($rootScope, $compile, $templateCache) {
            _scope = $rootScope.$new();
            _element = angular.element("<div checklist></div>");

            $templateCache.put('modules/components/checklist/checklist.html', window.__html__['app/modules/components/checklist/checklist.html']);
            $templateCache.put('modules/components/checklist/checklistItem.html', window.__html__['app/modules/components/checklist/checklistItem.html']);



            $compile(_element)(_scope);
            _scope.$digest();

        }));

        it("should compile correctly", function() {
            console.log(_element.html());
        });
    });
});
    */